name: deploy

on:
  workflow_run:
    workflows: ["ci"]
    branches: {}
    types:
      - completed


jobs:
  deploy:
    runs-on: [ubuntu-latest]
    steps:
      - name: echo
        uses: echo this is deploy run
